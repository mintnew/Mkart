apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: 317105901372.dkr.ecr.us-east-1.amazonaws.com/ecommerce-frontend:latest
          ports:
            - containerPort: 80